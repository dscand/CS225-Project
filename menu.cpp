namespace MouseFunctions {
	bool mouseOver(int x, int y, int width, int height) {
		int mouseX, mouseY;
		SDL_GetMouseState( &mouseX, &mouseY );

		// TODO: return true if mouse inside dimentions

		return false;
	}
}